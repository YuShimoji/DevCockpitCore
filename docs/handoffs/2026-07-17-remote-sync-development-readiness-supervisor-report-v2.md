# DevCockpitCore Authority Consolidation Historical Record V2

document_status: point_in_time_historical_evidence
evidence_window: 2026-07-13_to_2026-07-19
live_authority: false
normal_human_entry: docs/PROJECT_COCKPIT.md
machine_restart_projection: docs/runtime-state.md
durable_boundary: docs/project-context.md

## Authority Notice

This dated record preserves the distinct evidence and decisions that led to the
single repository re-entry route. It is not a current-state controller. Branch,
revision, pull-request, parity, worktree, and validation facts age immediately
and must be read from Git, tests, and generated evidence at the time of use.

Normal resumption does not require this file. Start with
`docs/PROJECT_COCKPIT.md`, then `docs/runtime-state.md`, and consult
`docs/project-context.md` when architecture or capability boundaries matter.
Use this record only when the historical rationale is needed.

## Consolidated Evidence Lineage

| Historical checkpoint | Evidence retained | Meaning now |
| --- | --- | --- |
| `e5ab070` local readiness | Python 3.11, 349 tests, 4/4 adapters, default pack 15 pass / 1 known warning / 0 fail | early point-in-time restart proof; not current health |
| `cf57191` on `7b914b4` | 382 tests and the same pack boundary after packet-integrity integration | proved that preserved context did not revive competing authority |
| `f2ae5b1` mainline | strict packet/report key surfaces, typed `task.next_state`, canonical UTF-8 LF binding | H1 ingress and checkout transport boundary |
| remote publication drill | a new non-force branch preserved rebased context; a depth-1 checkout reproduced Python 3.11 bootstrap and the 15 pass / 1 warning / 0 fail pack result | proved remote portability without rewriting the older branch |
| `84127ca` readiness refresh | Python 3.11.14, 405 tests, 4/4 adapters, default pack 15 pass / 1 known warning / 0 fail | last evidence captured in the superseded PR transport; revalidate live before use |

The early `e5ab070` run also generated a temporary Evidence Freshness receipt
with four fresh and four stale sources. Its authority was
`point_in_time_non_live`; it demonstrated evaluator behavior but never
authorized a current repository or project claim. The tracked supervision
packet likewise remains deterministic evidence for two fictional projects and
four reports, not authentic multi-project coverage.

## Retained Decisions

- `docs/PROJECT_COCKPIT.md` is the human navigation entry,
  `docs/runtime-state.md` is the bounded machine projection, and
  `docs/project-context.md` owns durable mission and capability boundaries.
- Earlier uppercase capsules and dated restart documents were removed as
  competing current authorities. Their useful reasoning is summarized here
  rather than restored as a second route.
- A / Priority Review Console is selected and user accepted. The same visual
  and comprehension gate is closed.
- Review actions remain `executable: false`; global rank is attention priority,
  never execution order.
- H1 strict manifest ingress and checkout transport are closed. Invalid UTF-8,
  bare carriage returns, path or hash drift, unexpected keys, and invalid
  continuation fields fail closed.
- Tracked dashboard, receipt, packet, and capture artifacts remain deterministic
  point-in-time or non-live evidence.
- No general runner, scheduler, watcher, external notification, credentials,
  database, target-repository writeback, C5, or C6 is authorized.

## Consolidation Disposition

The following redundant reports were removed from the normal repository tree
after their distinct evidence was folded into this record:

- `2026-07-13-remote-sync-development-readiness-supervisor-report-v1.md`
- `2026-07-15-remote-sync-development-readiness-supervisor-report-v1.md`
- `2026-07-17-remote-publication-resume-handoff-v1.md`

The first report supplied the early validation and freshness lineage. The
second supplied the latest-main rebase and Python 3.11 / 405-test evidence. The
third supplied the non-force publication and depth-1 restart drill. Their
branch, pull-request, head, parity, and command-routing metadata was deliberately
not retained as current control.

The older superseded pull-request branch also proposed shared frontmatter fields
for transient validation commit, timestamp, result, and report path. Those
fields were not retained because they would make Cockpit/runtime carry stale
workflow metadata. Its useful historical readiness evidence is represented in
the lineage above, and Git keeps the full patch history when forensic detail is
needed.

## Successor Candidate: H2

- Purpose: prove one authentic current report through explicit manifest
  binding, normalization, gate classification, packet generation and reload,
  and the accepted console.
- Effect: replace the remaining single-report live-ingress uncertainty with
  observed evidence without claiming portfolio-wide readiness.
- Requirements: one exact owner-authorized `AGENT_REPORT`, stable
  `project_key`, authority basis, source revision and observation context,
  permission for observer-only local artifacts, and negative readback tests.
- State: data-only successor candidate; input-gated and not started by this
  consolidation.
- Owner: Supervisor or user supplies and authorizes the input; an Agent may then
  perform the observer-only round-trip.
- Next move: remain at the current ready boundary until all required input facts
  are explicit. Do not search sibling repositories, infer a report from chat or
  fixtures, or promote deterministic evidence to authentic/live authority.

No H3-or-later roadmap in this historical file controls repository work.
Further horizons require evidence from H2 and a separate current decision.

## Known Quality Debt

- Purpose: preserve residue-detector coverage while distinguishing its
  historical pseudo-Git-tag fixture warning.
- Effect: the default validation pack remains accurately yellow rather than
  being mislabeled failed or fully green.
- Requirements: do not weaken the detector or change the fixture merely as part
  of authority integration.
- State: non-blocking maintenance debt.
- Owner: a separately authorized maintenance slice.
- Revisit trigger: the warning masks a real release gate or maintenance is
  explicitly prioritized.

## Historical Boundary

This consolidation changed documentation authority only. It did not change
source, schemas, adapters, packet or dashboard implementation, generated
samples, captures, dependencies, or execution capability. It did not obtain or
fabricate an external report and did not create an authentic/live claim.
