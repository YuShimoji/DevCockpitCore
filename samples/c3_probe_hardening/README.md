# C3 Probe Hardening Samples

This directory contains the canonical C3 probe hardening artifact.

`c3_probe_hardening_v1.json` ties together the non-canonical during-work sample,
the canonical post-commit probe result, and the canonical review result. It is
an acceptance package for C3 only; C4-C6 remain locked.
