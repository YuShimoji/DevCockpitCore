# C3 Second Command Design Samples

This directory contains design-only decision packets for possible future C3
command-key expansion.

The current packet does not implement a second command. It only recommends a
candidate for later Supervisor review.
