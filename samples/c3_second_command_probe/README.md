# C3 Second Command Probe Samples

This directory contains bounded help-probe evidence for C3 second-command
candidates.

The current sample is a proof packet for `adapters_validate_help`. It does not
add the command to the production controlled runner allowlist.
