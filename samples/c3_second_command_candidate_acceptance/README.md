# C3 second command candidate acceptance samples

This directory contains the option-B continuation packet for C3 second-command
candidate acceptance.

The current packet marks `adapters_validate_help` as a help-only accepted
candidate while preserving `status_snapshot_help` as the only production
accepted C3 command key.
