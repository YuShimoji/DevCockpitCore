# Status Snapshot Samples

This directory contains example output from the read-only status producer.

Sample files are observations, not project truth. If the referenced target
repository is absent on a machine, the sample may intentionally show a
structured warning snapshot instead of a successful git inspection.
